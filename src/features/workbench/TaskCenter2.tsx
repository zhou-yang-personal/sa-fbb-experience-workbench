/**
 * Legacy placeholder retained for stale imports.
 * Current task flow is represented by the WorkbenchAppV2 center panels.
 */
export function TaskCenter2() {
  return <section className="panel form-panel empty-panel"><h2>Legacy Task Center</h2><p>Use WorkbenchAppV2 centers instead.</p></section>;
}
